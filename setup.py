from setuptools import setup, find_packages

setup(
    name='tutorial-test-package',  # Replace with your actual package name
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Your project dependencies
    ],
)
